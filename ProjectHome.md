Lua wrapper around the avconv command to embed it inside lua scripts.

Allows to call avconv within the running Lua process (or "parent" process running Lua) and integrates with the Lua exception mechanism.